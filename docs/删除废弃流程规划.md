# 删除废弃流程规划

> **目标：** 删除 4 个废弃流程（动作迁移、素材创作、图生视频、快速创作）的全部前后端代码，仅保留「数字人」流程。
>
> **保留视图：** `modern_ui/src/views/DigitalHumanView.vue`

---

## 一、需要删除的文件清单（共 14 个）

### 1.1 前端 Vue 视图文件

| # | 文件路径 | 说明 |
|---|---------|------|
| 1 | `modern_ui/src/views/ActionTransferView.vue` | 动作迁移视图 |
| 2 | `modern_ui/src/views/AssetBasedView.vue.old` | 素材创作视图（.old 废弃） |
| 3 | `modern_ui/src/views/I2vView.vue` | 图生视频视图 |
| 4 | `modern_ui/src/views/QuickCreateView.vue` | 快速创作视图 |

### 1.2 前端 Vue 组件文件

| # | 文件路径 | 说明 |
|---|---------|------|
| 5 | `modern_ui/src/components/ActionTransferForm.vue` | 动作迁移表单组件 |
| 6 | `modern_ui/src/components/AssetBasedForm.vue` | 素材创作表单组件 |
| 7 | `modern_ui/src/components/I2vForm.vue` | 图生视频表单组件 |
| 8 | `modern_ui/src/components/QuickCreateForm.vue` | 快速创作表单组件 |

### 1.3 前端 TypeScript Pipeline 定义

| # | 文件路径 | 说明 |
|---|---------|------|
| 9 | `modern_ui/src/pipelines/action_transfer.ts` | 动作迁移 pipeline |
| 10 | `modern_ui/src/pipelines/asset_based.ts` | 素材创作 pipeline |
| 11 | `modern_ui/src/pipelines/i2v.ts` | 图生视频 pipeline |
| 12 | `modern_ui/src/pipelines/standard.ts` | 快速创作 pipeline |

### 1.4 后端 Python 文件

| # | 文件路径 | 说明 |
|---|---------|------|
| 13 | `pixelle_video/pipelines/asset_based.py` | 素材创作后端 pipeline |
| 14 | `pixelle_video/prompts/asset_script_generation.py` | 素材创作提示词 |

## 二、需要修改的文件清单（共 9 个）

### 2.1 前端类型定义 - types.ts

**文件：** `modern_ui/src/types.ts`

删除以下 4 个接口定义：

| 接口名 | 约行范围 | 说明 |
|--------|---------|------|
| `QuickForm` | L53-86 | 快速创作表单类型 |
| `AssetForm` | L88-107 | 素材创作表单类型 |
| `I2vForm` | L181-186 | 图生视频表单类型 |
| `ActionForm` | L188-195 | 动作迁移表单类型 |

> ⚠️ **保留：** `DigitalForm`、`SubtitleConfig`、`VideoApiParams`、`DigitalWorkflowConfig` 等被数字人使用的接口不变。

### 2.2 前端 Pipeline Barrel 导出

**文件：** `modern_ui/src/pipelines/__init.ts`

1. 删除 4 行 export：
   - `export * from './standard'`
   - `export * from './asset_based'`
   - `export * from './i2v'`
   - `export * from './action_transfer'`
2. 从 `ALL_PIPELINES` 数组删除 4 项：
   - `'quick_create'`、`'custom_media'`、`'image_to_video'`、`'action_transfer'`
3. 只保留：`export * from './digital_human'` 和 `ALL_PIPELINES = ['digital_human']`

### 2.3 前端工具列表 - base.ts

**文件：** `modern_ui/src/pipelines/base.ts`

1. `PIPELINE_TOOLS` 数组 → 仅保留 `digital_human` 条目（删除其他 4 个）
2. `PIPELINE_NAMES` 对象 → 仅保留 `digital_human: '数字人'`（删除其他 4 个）

### 2.4 后端 API 路由

**文件：** `api/routers/pipelines.py`

| 操作 | 内容 |
|------|------|
| 删除 import | `from pixelle_video.pipelines.asset_based import AssetBasedPipeline`（L40） |
| 删除 Model | `AssetBasedRequest` 类（~L59-74） |
| 删除 Model | `ImageToVideoRequest` 类（~L77-81） |
| 删除 Model | `ActionTransferRequest` 类（~L84-90） |
| 删除端点 + 逻辑 | `@router.post("/asset-based/async")` + `generate_asset_based_async()`（~L628-702） |
| 删除端点 + 逻辑 | `@router.post("/image-to-video/async")` + `generate_image_to_video_async()`（~L705-778） |
| 删除端点 + 逻辑 | `@router.post("/action-transfer/async")` + `generate_action_transfer_async()`（~L781-864） |

> ⚠️ **保留（属于数字人）：** `DigitalHumanRequest`、`DigitalWorkflowConfig`、`SubtitleRequestConfig`、工具函数 `_run_tts()`、`_burn_subtitles_sync()`、`_run_digital_human_pipeline()`、`_is_api_workflow()`、`_execute_comfy_video_workflow()`、`_download_to_file()`、`_task_response()`，以及端点 `@router.post("/digital-human/subtitle-preview")` 和 `@router.post("/digital-human/async")`。

### 2.5 后端 Pipeline __init__.py

**文件：** `pixelle_video/pipelines/__init__.py`

- 删除：`from pixelle_video.pipelines.asset_based import AssetBasedPipeline`（L24）
- 从 `__all__` 列表中删除 `"AssetBasedPipeline"`（L32）

### 2.6 后端 service.py

**文件：** `pixelle_video/service.py`

- 删除：`from pixelle_video.pipelines.asset_based import AssetBasedPipeline`（L41）
- 删除：`"asset_based": AssetBasedPipeline(self)` 注册行（L397）

### 2.7 前端 SPA 版本 app.js（遗留旧版）

**文件：** `modern_ui/app.js`

| 区域 | 操作 |
|------|------|
| componentTemplates | 删除 4 个模板：`quickCreate`、`assetBased`、`i2v`、`actionTransfer` |
| 组件注册 | 删除 `QuickCreateForm`、`AssetBasedForm`、`I2vForm`、`ActionTransferForm` 的 const 定义和 components 注册 |
| data 表单状态 | 删除 `quickForm`、`assetForm`、`i2vForm`、`actionForm` |
| tools 列表 | 只保留 `digital_human`，删除其他 4 个 |
| computed 属性 | 删除 `i2vWorkflows`、`actionWorkflows` |
| mounted 初始化 | 删除对 `i2vForm`、`actionForm` 的默认值设置 |
| generateCurrent() | 仅保留 `digital_human` handler |
| 各 generate 方法 | 删除 `generateQuickCreate`、`generateAssetBased`、`generateI2v`、`generateActionTransfer` |
| onHistorySelect | 删除引用 `actionForm`、`assetForm`、`i2vForm` 的分支 |
| currentAssetRows | 仅保留 `digital_human` 分支 |
| currentWorkflowRows | 仅保留 `digital_human` 分支 |

### 2.8 文档 - 完整流程.md

**文件：** `docs/完整流程.md`

- 目录中删除 4 个条目，仅保留数字人
- 删除章节：快速创作、自定义素材、图生视频、动作迁移
- 仅保留 `## 3. 🤖 数字人口播` 章节

---

## 三、不需要修改的文件

| 文件 | 原因 |
|------|------|
| `modern_ui/src/router/index.ts` | 路由中只有 `DigitalHumanView.vue` 被引用，无这 4 个视图的路由 |
| `modern_ui/styles.css` | CSS 注释包含功能名但不影响功能，共享样式类无需删除 |
| `modern_ui/src/views/AppLayout.vue` | 侧边栏导航只有 create(数字人)、tasks、history、account |
| `modern_ui/src/views/HomePage.vue` | features 只展示数字人功能 |
| `pixelle_video/services/api_media.py` | `action_transfer` 等是能力配置描述字符串，不产生运行时错误 |
| `pixelle_video/services/media.py` | `i2v_` 前缀过滤是通配逻辑，保留无害 |
| `docs/数字人视频字幕功能方案.md` | 仅涉及数字人 |

---

## 四、执行顺序

```
Step 1: 删除 14 个纯文件
Step 2: 修改 types.ts + pipelines/__init.ts + base.ts
Step 3: 修改后端 api/routers/pipelines.py + pipelines/__init__.py + service.py
Step 4: 修改遗留 app.js
Step 5: 更新 docs/完整流程.md
Step 6: 验证：编译无报错，数字人流程正常
```

---

## 五、验证清单

- [ ] `pnpm run dev` 正常启动
- [ ] `npx vue-tsc --noEmit` 无类型错误
- [ ] `/create` 数字人页面正常访问
- [ ] 数字人表单提交功能正常
- [ ] 侧边栏导航正常
- [ ] 任务中心 / 历史记录页面不受影响
- [ ] `pnpm run build` 无构建错误

